schema_version = 1
plugin_id = "local.contract.handler"
version = "0.1.0"
requires_core = ">=3.1.0.dev2,<4"
entry = "plugin.py"
requires_plugins = ["local.contract.source"]
provides = ["local.contract.handler"]
requires_distributions = []
