schema_version = 1
plugin_id = "local.contract.combined"
version = "0.1.0"
requires_core = ">=3.1.0.dev2,<4"
entry = "plugin.py"
requires_plugins = []
provides = ["local.contract.combined.events", "local.contract.combined.handler"]
requires_distributions = []
